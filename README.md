# Telegram Парсер и Бот

Система для парсинга Telegram-канала и обработки заявок через Telegram-бота.

## Описание проекта

Проект состоит из двух основных компонентов:

1. **Парсер Telegram-канала** - автоматически отслеживает новые сообщения в указанном канале, извлекает ключевую информацию (тип услуги, описание, дата) и сохраняет в базу данных.

2. **Telegram-бот** - предоставляет интерфейс для:
    - Просмотра спарсенных постов из канала
    - Создания заявок через диалог с валидацией данных
    - Просмотра статистики заявок
    - Управления статусами заявок (для администраторов)

## Установка и настройка

### 1. Клонирование и установка зависимостей

```bash
# Установка Poetry (если еще не установлен)
curl -sSL https://install.python-poetry.org | python3 -

# Или под Mac OS
brew install poetry

# Установка зависимостей проекта
poetry install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Заполните следующие переменные:

```env
# Telegram API credentials (получите на https://my.telegram.org/apps)
API_ID=your_api_id
API_HASH=your_api_hash

# Telegram Bot Token (получите у @BotFather)
BOT_TOKEN=your_bot_token

# ID Telegram-канала для парсинга (например: -1001234567890)
CHANNEL_ID=your_channel_id

# ID пользователей для уведомлений (получите у @userinfobot)
MANAGER_ID=your_manager_id
LEADER_ID=your_leader_id

# Путь к файлу сессии Telethon (опционально)
SESSION_NAME=telegram_session
```

#### Как получить необходимые данные:

1. **API_ID и API_HASH**:

    - Перейдите на https://my.telegram.org/apps
    - Войдите с вашим номером телефона
    - Создайте новое приложение
    - Скопируйте `api_id` и `api_hash`

2. **BOT_TOKEN**:

    - Найдите @BotFather в Telegram
    - Отправьте команду `/newbot`
    - Следуйте инструкциям для создания бота
    - Скопируйте полученный токен

3. **CHANNEL_ID**:

    - Добавьте бота в канал как администратора
    - Используйте бота @userinfobot для получения ID канала
    - Или используйте формат: `-1001234567890` (отрицательное число)

4. **MANAGER_ID и LEADER_ID**:
    - Используйте бота @userinfobot для получения вашего ID
    - Или используйте бота @getidsbot

## Запуск проекта

### Запуск через Poetry

```bash
poetry run python main.py
```

### Или активируйте виртуальное окружение

```bash
poetry shell
python main.py
```

При первом запуске Telethon попросит вас авторизоваться:

1. Введите ваш номер телефона
2. Введите код подтверждения из Telegram
3. При необходимости введите пароль двухфакторной аутентификации

## Использование

### Команды бота

-   `/start` - Начать работу с ботом
-   `/help` - Показать справку
-   `/posts` - Просмотреть посты из канала
-   `/stats` - Статистика заявок

### Создание заявки

1. Нажмите кнопку "📋 Создать заявку" или отправьте `/start`
2. Введите ваше имя
3. Введите контакт (email, телефон или Telegram username)
4. Опишите задачу

Заявка автоматически будет отправлена руководителю и менеджеру.

### Просмотр постов

Используйте кнопку "📰 Просмотреть посты" или команду `/posts` для просмотра спарсенных постов из канала.

### Статистика

Администраторы (руководитель и менеджер) могут:

-   Просматривать полную статистику заявок
-   Изменять статусы заявок (Новая → В работе → Завершена)
